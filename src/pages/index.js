import Input from '../components/commons/Input';

const Home = () => (
  <div>
    <div>Olá :)</div>
    <Input name="name" placeholder="Text input placeholder..." />
  </div>
);

export default Home;
