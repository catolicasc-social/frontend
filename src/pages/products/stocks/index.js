import ContainerProducts from '../../../layouts/products';

const Stocks = () => (
  <ContainerProducts>
    <span>Estoque</span>

    <style jsx>
      {`
        span {
          color: red;
        }
      `}
    </style>
  </ContainerProducts>
);

export default Stocks;
