import { FilePicker } from 'evergreen-ui';

const FilePickerCustom = ({ ...props }) => <FilePicker {...props} />;

// eslint-disable-next-line import/prefer-default-export
export { FilePickerCustom as FilePicker };
