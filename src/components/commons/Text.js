import { Text } from 'evergreen-ui';

const TextCustom = ({ children, size }) => <Text size={size}>{children}</Text>;

// eslint-disable-next-line import/prefer-default-export
export { TextCustom as Text };
