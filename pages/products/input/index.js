import ContainerProducts from '../../../layouts/products';

const Input = () => (
  <ContainerProducts>
    <span>Entrada</span>

    <style jsx>
      {`
        span {
          color: red;
        }
      `}
    </style>
  </ContainerProducts>
);

export default Input;
